
---
platform: linux
image_resource:
  type: registry-image
  source:
    repository: postman/newman
    tag: 5-alpine

params:
  COLLECTION_URL: repo/postman/Mockito.json
  #ENVIRONMENT_URL: repo/postman/service_local.postman_environment.json
  NEWMAN_ADDITIONAL_ARGS: -r htmlextra

inputs:
  - name: repo
  #- tasks: tasks

outputs:
  - name: reports

run:
  path: sh
  args:
    - repo/concourse_ci/local-scripts/publish-report-postman.sh